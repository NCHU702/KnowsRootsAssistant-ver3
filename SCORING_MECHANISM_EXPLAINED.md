# 🔍 混合檢索評分機制詳解

## 📊 評分計算流程

### 第一步：語義搜尋（Semantic Search）

使用 Embedding 模型計算查詢與論文摘要的向量相似度。

```
語義分數 (sem_score) = cosine_similarity(query_embedding, paper_embedding)
範圍: 0.0 ~ 1.0
```

**特點**：
- 理解語義含義，不需要精確字詞匹配
- 能找到概念相近但用詞不同的論文
- 例如：查詢「深度學習」能找到「神經網路」相關論文

---

### 第二步：關鍵詞搜尋（BM25 Keyword Search）

使用 BM25 演算法計算關鍵詞匹配程度。

#### 2.1 詞彙增強（Term Boosting）

**如果啟用 LLM 分析器**：
```python
# LLM 分析查詢詞彙
核心詞（重要） → 重複 3 次
廣泛詞（不重要） → 移除

例如：
原始查詢: ["深度學習", "在", "鼻腔癌", "的", "應用"]
LLM 分析: 核心詞=["深度學習", "鼻腔癌"], 廣泛詞=["應用"]
增強後: ["深度學習", "深度學習", "深度學習", "鼻腔癌", "鼻腔癌", "鼻腔癌"]
```

**如果使用字典系統**：
```python
# 查字典判斷詞彙類型
領域詞（專業術語） → 重複 3 次
泛用詞（常見詞） → 保持原樣

例如：
原始查詢: ["CNN", "模型", "訓練"]
字典判斷: 領域詞=["CNN"], 泛用詞=["模型", "訓練"]
增強後: ["CNN", "CNN", "CNN", "模型", "訓練"]
```

#### 2.2 BM25 計算

```
BM25_score = Σ IDF(詞) × (tf × (k1 + 1)) / (tf + k1 × (1 - b + b × (文檔長度 / 平均文檔長度)))

其中：
- tf = 詞頻（term frequency）
- IDF = 逆文檔頻率（inverse document frequency）
- k1 = 1.5（控制詞頻飽和度）
- b = 0.75（控制文檔長度懲罰）
```

#### 2.3 正規化

```python
normalized_bm25_score = bm25_score / max(所有 BM25 分數)
範圍: 0.0 ~ 1.0
```

**特點**：
- 精確字詞匹配，必須包含查詢詞彙
- 重視罕見詞（IDF 高），忽略常見詞（IDF 低）
- 核心詞重複 3 次 → 提高匹配權重

---

### 第三步：加權組合（Weighted Combination）

#### 3.1 基礎組合分數

```python
combined_score = semantic_weight × sem_score + keyword_weight × key_score
```

**預設權重**：
- `semantic_weight = 0.90` (90%)
- `keyword_weight = 0.10` (10%)

**自適應調整**（AdaptiveWeightAdjuster）：

| 查詢特徵 | 語義權重變化 | 關鍵詞權重變化 |
|---------|-------------|---------------|
| 短查詢（<5詞） | 維持 0.90 | 維持 0.10 |
| 長查詢（≥10詞） | +0.05 | -0.05 |
| 包含專有名詞（CNN, LSTM等） | +0.05 | -0.05 |
| 包含否定詞（不是、沒有等） | +0.05 | -0.05 |

**LLM 智能調整**：

根據 LLM 分析結果動態調整：

```python
if 廣泛詞數量多 or 明確領域:
    semantic_weight → 0.98
    keyword_weight → 0.02
    
if 核心詞數量多且無廣泛詞:
    semantic_weight → 0.70
    keyword_weight → 0.30
```

#### 3.2 核心詞匹配獎勵（優化版）

**舊版問題**：
```python
# 舊版：無條件給獎勵
if match_score > 0.5:
    bonus = 0.2 × match_score  # 最多 +0.2
    combined_score += bonus
```

**問題案例**：
```
查詢：「如何應用深度學習在鼻腔癌」
論文A：「天際線路徑查詢」(語義 0.4755)
  → 匹配到「應用」、「查詢」等泛用詞
  → match_score = 0.95
  → bonus = 0.2 × 0.95 = 0.19
  → 最終分數 = 0.4755 + 0.19 = 0.6655 ❌ 錯誤！

論文B：「基於深度學習之鼻咽癌腫塊辨識」(語義 0.5027)
  → bonus = 0
  → 最終分數 = 0.5027 ✓ 正確但排名第二
```

**新版：雙重條件檢查**

```python
if match_score > 0.5 AND sem_score >= 0.45:
    # 獎勵與語義分數成正比
    bonus = 0.15 × match_score × (sem_score / 0.5)
    combined_score += bonus
```

**新版效果**：
```
論文A：「天際線路徑查詢」(語義 0.4755)
  → match_score = 0.95 ✓
  → sem_score = 0.4755 ✓ (≥ 0.45)
  → bonus = 0.15 × 0.95 × (0.4755/0.5) = 0.135
  → 最終分數 = 0.4755 + 0.135 = 0.6105

論文B：「鼻咽癌腫塊辨識」(語義 0.5027)
  → match_score = 0.85
  → sem_score = 0.5027 ✓
  → bonus = 0.15 × 0.85 × (0.5027/0.5) = 0.128
  → 最終分數 = 0.5027 + 0.128 = 0.6307 ✓ 排名第一
```

**核心改進**：
1. ✅ 要求語義分數 ≥ 0.45（避免完全不相關的論文）
2. ✅ 獎勵與語義分數成正比（語義越高，獎勵越多）
3. ✅ 降低最大獎勵：0.2 → 0.15（減少過度提升）

---

## 📈 完整計算範例

### 範例：查詢「如何應用深度學習在鼻腔癌」

#### 論文A：「基於深度學習之鼻咽癌腫塊辨識」

**步驟 1：語義搜尋**
```
sem_score = 0.5027
```

**步驟 2：關鍵詞搜尋**
```
查詢詞彙: ["如何", "應用", "深度學習", "在", "鼻腔癌"]
LLM 分析: 核心詞=["深度學習", "鼻腔癌"], 廣泛詞=["應用"]
增強後: ["深度學習" × 3, "鼻腔癌" × 3]

論文標題包含: "深度學習", "鼻咽癌"（與鼻腔癌相似）
BM25 原始分數: 8.45
正規化後: 8.45 / 10.2 = 0.828
key_score = 0.828
```

**步驟 3：加權組合**
```
LLM 智能調整: semantic_weight=0.98, keyword_weight=0.02

combined_score = 0.98 × 0.5027 + 0.02 × 0.828
               = 0.4926 + 0.0166
               = 0.5092
```

**步驟 4：核心詞匹配獎勵**
```
查詢核心詞: ["深度學習", "鼻腔癌"]
論文包含: ["深度學習", "鼻咽癌", "辨識", "腫塊"]
匹配率: 2/2 = 1.0 (100%)

match_score = 1.0 ✓ (> 0.5)
sem_score = 0.5027 ✓ (≥ 0.45)

bonus = 0.15 × 1.0 × (0.5027/0.5) = 0.151
```

**最終分數**
```
final_score = 0.5092 + 0.151 = 0.6602 ⭐ 第一名
```

#### 論文B：「查詢3_近似巡訪天際線路徑查詢」

**步驟 1：語義搜尋**
```
sem_score = 0.4755
```

**步驟 2：關鍵詞搜尋**
```
查詢詞彙: 同上
增強後: ["深度學習" × 3, "鼻腔癌" × 3]

論文標題包含: "查詢"（通用詞）
BM25 原始分數: 1.2
正規化後: 1.2 / 10.2 = 0.118
key_score = 0.118
```

**步驟 3：加權組合**
```
combined_score = 0.98 × 0.4755 + 0.02 × 0.118
               = 0.4660 + 0.0024
               = 0.4684
```

**步驟 4：核心詞匹配獎勵**
```
查詢核心詞: ["深度學習", "鼻腔癌"]
論文包含: ["巡訪", "天際線", "路徑", "查詢", "近似"]
匹配率: 0/2 = 0.0 (0%)

match_score = 0.0 ✗ (≤ 0.5)
→ 不符合條件，無獎勵
```

**最終分數**
```
final_score = 0.4684 (第二名，正確！)
```

---

## 🎯 評分機制總結

### 評分公式

```
最終分數 = 基礎組合分數 + 核心詞匹配獎勵

基礎組合分數 = semantic_weight × sem_score + keyword_weight × key_score

核心詞匹配獎勵 = {
    0.15 × match_score × (sem_score / 0.5),  if match_score > 0.5 AND sem_score ≥ 0.45
    0,                                         otherwise
}
```

### 分數範圍

| 組成部分 | 最小值 | 最大值 | 典型值 |
|---------|--------|--------|--------|
| 語義分數 | 0.0 | 1.0 | 0.3 ~ 0.7 |
| 關鍵詞分數 | 0.0 | 1.0 | 0.0 ~ 0.8 |
| 基礎組合 | 0.0 | 1.0 | 0.4 ~ 0.6 |
| 匹配獎勵 | 0.0 | 0.15 | 0.0 ~ 0.10 |
| **最終分數** | **0.0** | **1.15** | **0.4 ~ 0.7** |

### 權重配置

**預設配置**：
```python
semantic_weight = 0.90  # 90% 重視語義理解
keyword_weight = 0.10   # 10% 重視關鍵詞匹配
```

**自適應調整範圍**：
```python
semantic_weight: 0.65 ~ 0.98
keyword_weight: 0.02 ~ 0.35
```

---

## ⚠️ 已知問題與解決方案

### 問題 1：不相關論文獲得高分（已修復）

**原因**：核心詞匹配獎勵無條件給予，導致低語義分數論文被提升

**解決**：
- ✅ 要求 `sem_score ≥ 0.45`
- ✅ 獎勵與語義分數成正比
- ✅ 降低最大獎勵：0.20 → 0.15

### 問題 2：醫療查詢找不到結果（已修復）

**原因**：醫療詞彙（鼻咽癌）被錯誤切分成「鼻」+「咽」+「癌」

**解決**：
- ✅ 建立醫療詞典（573個詞彙）
- ✅ 整合到 jieba 分詞器
- ✅ 整合到 LLM 詞彙分析器

### 問題 3：查詢類型識別錯誤（已修復）

**原因**：「如何應用」被識別為 `application`（應用概覽），而非 `methodology`（方法論）

**解決**：
- ✅ 優先檢測「如何」、「怎麼」等方法論關鍵詞
- ✅ 正確識別為 `methodology` → 觸發 Layer 2

---

## 🔧 配置建議

### 一般查詢（概覽、列表）
```python
semantic_weight = 0.90
keyword_weight = 0.10
threshold = 0.48
```

### 專業查詢（模型、方法論）
```python
semantic_weight = 0.70
keyword_weight = 0.30
threshold = 0.50
use_layer2 = True  # 獲取詳細內容
```

### 醫療領域查詢
```python
semantic_weight = 0.98  # 高度依賴語義理解
keyword_weight = 0.02
medical_dict = True     # 啟用醫療詞典
```

---

## 📝 參考資料

- **BM25 演算法**：Robertson & Zaragoza (2009). "The Probabilistic Relevance Framework: BM25 and Beyond"
- **混合檢索**：Gao et al. (2021). "Hybrid Retrieval for Open-Domain Question Answering"
- **查詢擴展**：Azad & Deepak (2019). "Query Expansion Techniques for Information Retrieval: a Survey"
