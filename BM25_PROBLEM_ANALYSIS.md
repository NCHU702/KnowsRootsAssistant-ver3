# BM25 關鍵詞匹配問題分析

## 問題現象

### 案例 1: "深度學習在醫療的應用是什麼？"

**期望**: 「鼻咽癌腫塊辨識」應該排第一（真正的醫療論文）

**實際結果**:
1. ❌ 標準9_PM2.5預測 [0.741] - **不是醫療！**
2. ✓ 鼻咽癌腫塊辨識 [0.638] - 醫療相關

**原因分析**:
```
查詢分詞: ['深度', '學習', '醫療', '應用', '什麼']

PM2.5 論文標題: "應用三維高斯函數結合深度學習之區域 PM2.5 預測"
匹配詞: ✓深度 ✓學習 ✗醫療 ✓應用 ✗什麼
匹配率: 3/5 = 60%
BM25分數: 1.000 (最高) → 組合分數: 0.741

鼻咽癌論文標題: "基於深度學習之鼻咽癌腫塊辨識"
匹配詞: ✓深度 ✓學習 ✗醫療 ✗應用 ✗什麼
匹配率: 2/5 = 40%
BM25分數: 0.797 → 組合分數: 0.638
```

### 案例 2: "深度學習在人流的應用"

**期望**: 人流相關論文應該排前面

**實際結果**:
1. ❌ 標準9_PM2.5預測 [0.752] - **不是人流！**
2. ❌ 鼻咽癌腫塊辨識 [0.647] - **不是人流！**
3. ✓ 標準12_人流預測 [0.465]
4. ✓ 標準11_人流預測 [0.461]

**原因分析**:
```
查詢分詞: ['深度', '學習', '人流', '應用']

PM2.5 論文: "應用三維高斯函數結合深度學習之區域 PM2.5 預測"
匹配詞: ✓深度 ✓學習 ✗人流 ✓應用
匹配率: 3/4 = 75%
BM25分數: 1.000

人流論文: "基於R-tree與SPACE-MDL-LSTM提升大區域人流預測之效率"
匹配詞: ✗深度 ✗學習 ✓人流 ✗應用 (但有"預測")
匹配率: 1/4 = 25%
BM25分數: 0.422
```

### 案例 3: "人流預測" (效果較好)

**實際結果**:
1. ✓ 標準12_人流預測 [0.747]
2. ✓ 標準11_人流預測 [0.620]
3. ❌ 標準9_PM2.5預測 [0.354]

**為什麼這次正常？**
```
查詢分詞: ['人流', '預測']

人流論文: "基於R-tree與SPACE-MDL-LSTM提升大區域人流預測之效率"
匹配詞: ✓人流 ✓預測
匹配率: 2/2 = 100%
BM25分數: 1.000 ✓

PM2.5論文: "應用三維高斯函數結合深度學習之區域 PM2.5 預測"
匹配詞: ✗人流 ✓預測
匹配率: 1/2 = 50%
BM25分數: 0.238 ✓ 明顯較低
```

## 核心問題

### 問題 1: 泛用詞干擾

**「應用」、「深度學習」、「預測」**這些詞太常見，導致：
- 幾乎所有論文都包含這些詞
- BM25 會給包含更多這些詞的論文高分
- **真正的核心概念（醫療、人流）被稀釋**

### 問題 2: IDF 計算不準確

在只有 10 篇論文的小型語料庫中：
```
詞彙統計 (假設):
"深度學習" - 出現在 8/10 篇論文 → IDF 低（常見詞）
"應用" - 出現在 7/10 篇論文 → IDF 低
"人流" - 出現在 4/10 篇論文 → IDF 中等
"醫療" - 出現在 1/10 篇論文 → IDF 高（但標題中可能沒有！）

問題: "醫療"這個核心概念詞在論文標題/摘要中可能根本不出現！
例如: "鼻咽癌腫塊辨識" - 標題中沒有"醫療"二字
```

### 問題 3: 標題與查詢的詞彙不匹配

```
使用者查詢: "醫療"
論文標題用詞: "鼻咽癌"、"腫塊辨識"、"病患"

→ BM25 無法匹配！（因為詞彙完全不同）
→ 但語義上明顯相關（embedding 可以理解）
```

## 分數詳細比較

### "深度學習在人流的應用" 各論文得分

| 論文 | 語義 | 關鍵詞 | 組合 | 說明 |
|-----|------|-------|------|------|
| PM2.5預測 | 0.504 | **1.000** | **0.752** | 包含"深度學習+應用" |
| 鼻咽癌辨識 | 0.498 | 0.797 | 0.647 | 包含"深度學習" |
| 人流預測12 | **0.507** | 0.422 | 0.465 | ✓包含"人流" |
| 人流預測11 | **0.506** | 0.416 | 0.461 | ✓包含"人流" |

**觀察**:
- PM2.5 的**語義分數並不是最高** (0.504 vs 0.507)
- 但因為**關鍵詞分數極高** (1.000)，拉高了組合分數
- 真正的人流論文語義分數最高，但關鍵詞分數低

## 解決方案

### 方案 1: 降低泛用詞權重（推薦）

在 BM25 計算前過濾或降權常見詞：

```python
# 在 hybrid_retriever.py 的 _tokenize 方法中
COMMON_WORDS = ['深度', '學習', '應用', '基於', '研究', '方法', '分析', '預測']

def _tokenize(self, text: str) -> List[str]:
    tokens = list(jieba.cut(text.lower()))
    tokens = [t for t in tokens if len(t.strip()) > 1]
    
    # 過濾或降權常見詞
    # 方法1: 完全移除
    tokens = [t for t in tokens if t not in COMMON_WORDS]
    
    # 方法2: 重複重要詞（提高權重）
    # 例如: ['人流', '醫療'] 變成 ['人流', '人流', '醫療', '醫療']
    
    return tokens
```

### 方案 2: 動態調整權重（基於查詢特性）

```python
# 如果查詢包含領域特定詞（醫療、人流），提高關鍵詞權重
if '醫療' in query or '人流' in query:
    keyword_weight += 0.2  # 確保領域詞被重視
    
# 如果查詢包含太多泛用詞，降低關鍵詞權重
common_word_ratio = len([w for w in tokens if w in COMMON_WORDS]) / len(tokens)
if common_word_ratio > 0.6:
    keyword_weight -= 0.2  # 降低泛用詞的影響
```

### 方案 3: 詞彙擴展（查詢改寫）

```python
# 在查詢擴展時，加入領域同義詞
DOMAIN_SYNONYMS = {
    '醫療': ['醫學', '臨床', '診斷', '治療', '病患', '疾病'],
    '人流': ['人群', '行人', '客流', '人潮', '人數']
}

# 擴展查詢
original = "深度學習在醫療的應用"
expanded = "深度學習在醫療醫學臨床診斷的應用"
```

### 方案 4: 使用 TF-IDF 替代純 BM25

```python
# 調整 BM25 參數以減少詞頻影響
from rank_bm25 import BM25Okapi

# k1 控制詞頻飽和度 (預設 1.5)
# k1 越小，高頻詞的優勢越小
self.bm25 = BM25Okapi(tokenized_corpus, k1=0.5, b=0.75)
```

### 方案 5: 混合權重根據匹配度動態調整

```python
def adaptive_hybrid_scoring(query, doc, semantic_score, keyword_score):
    # 檢查是否匹配核心概念
    query_tokens = set(tokenize(query))
    doc_tokens = set(tokenize(doc.page_content))
    
    # 核心詞列表
    core_concepts = {'醫療', '人流', '交通', 'CNN', 'LSTM', ...}
    
    # 計算核心詞匹配率
    core_matches = query_tokens & doc_tokens & core_concepts
    core_in_query = query_tokens & core_concepts
    
    if core_in_query:
        match_ratio = len(core_matches) / len(core_in_query)
        
        if match_ratio > 0.5:
            # 核心詞匹配良好，提高關鍵詞權重
            weight_adjustment = 0.2
        else:
            # 核心詞未匹配，降低關鍵詞權重
            weight_adjustment = -0.2
    else:
        weight_adjustment = 0
    
    adjusted_keyword_weight = keyword_weight + weight_adjustment
    adjusted_semantic_weight = 1 - adjusted_keyword_weight
    
    return adjusted_semantic_weight * semantic_score + adjusted_keyword_weight * keyword_score
```

## 建議實施順序

1. **立即實施**: 方案 2 - 動態調整權重（最簡單）
2. **短期**: 方案 1 - 過濾泛用詞（中等難度，效果明顯）
3. **中期**: 方案 5 - 根據核心詞匹配動態評分（較複雜但精準）
4. **長期**: 方案 3 - 領域詞彙擴展（需要維護詞典）

## 效果預測

### 實施方案 1+2 後

**查詢**: "深度學習在醫療的應用"

```
過濾泛用詞後:
查詢分詞: ['人流'] (移除'深度'、'學習'、'應用')

PM2.5論文: "... PM2.5 預測"
匹配詞: ✗醫療
BM25分數: 0.0 → 大幅降低

鼻咽癌論文: "鼻咽癌腫塊辨識"  
匹配詞: ✗醫療 (但語義分數高)
檢測到領域詞"醫療" → 提高語義權重 0.7
組合分數: 0.7 × 0.498 + 0.3 × 0.0 = 0.349

等等，這樣還是有問題...
```

### 更好的方案: 方案 5 (核心詞匹配)

```
查詢: "深度學習在醫療的應用"
核心詞: ['醫療']

PM2.5論文: 標題不含"醫療" → 降低關鍵詞權重
組合: 0.7×0.504 + 0.3×1.0 = 0.653

鼻咽癌論文: 標題不含"醫療"但語義相關
檢測到"鼻咽癌"是醫療相關 → 提高語義權重
組合: 0.8×0.498 + 0.2×0.797 = 0.557

嗯...還是需要語義理解「鼻咽癌」屬於「醫療」
```

## 根本問題

**BM25 的本質限制**: 純粹的詞彙匹配，無法理解語義

- "鼻咽癌" ≠ "醫療" (詞彙不同)
- 但語義上相關（需要 embedding）

**混合搜尋的困境**:
- 提高語義權重 → 可以找到"鼻咽癌"是醫療
- 但同時也讓泛用詞的影響變小
- 需要更智能的權重策略

## 最終建議

**實施「核心概念增強」策略**:

```python
# 在查詢時
if has_domain_specific_terms(query):
    # 有領域特定詞（醫療、人流）
    # 1. 提高語義權重（理解概念）
    # 2. 在 BM25 中加重領域詞權重
    semantic_weight = 0.7
    keyword_weight = 0.3
    # 且在分詞時，領域詞重複出現
    tokens = ['醫療', '醫療', '醫療', '應用']
else:
    # 一般查詢，平衡
    semantic_weight = 0.5
    keyword_weight = 0.5
```

這樣可以:
1. ✓ 保持領域詞的重要性
2. ✓ 利用語義理解「鼻咽癌」是醫療
3. ✓ 降低泛用詞的干擾
